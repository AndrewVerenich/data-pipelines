version: 2

models:
  - name: dim_customer
    columns:
      - name: customer_sk
        tests: [not_null, unique]
  - name: dim_account
    columns:
      - name: account_sk
        tests: [not_null, unique]
  - name: dim_merchant
    columns:
      - name: merchant_sk
        tests: [not_null, unique]
  - name: dim_date
    columns:
      - name: date_sk
        tests: [not_null, unique]
  - name: dim_currency
    columns:
      - name: currency_sk
        tests: [not_null, unique]
